"""Rayen AI daemon (rayend).

The integrated AI brain for Rayen OS. Provides a hybrid (local + cloud)
language-model agent that can inspect and modify the system through a set
of guarded tools, exposed over a local HTTP API consumed by the CLI and the
desktop chat application.
"""

__version__ = "1.0.0"
